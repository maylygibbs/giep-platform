import { SelectOption } from './select-option';

export class ChargeDomainLevelPriority {
    id: number;
    charge: SelectOption;  // icargo
    domainLevel: SelectOption;  // nivel dominio
    priority: number;  // prioridad// prioridad
}
